# Pipeline

```{figure} img/pipeline.png
:alt: The input pipeline
:align: center

The report goes through the parser, then the pre-transform filters, get transformed by the output mode, then the post-transform filters, the output mode again, and finally the pointer.
```